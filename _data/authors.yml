# Only authors listed here get extra formatting (bold = you, url = profile link).
# Names that are not listed are rendered as plain text, which is the default for co-authors.

"Linke Fan":
  bold: true
